@using Microsoft.AspNetCore.Identity
@using _20230131_MVCIdentity.Areas.Identity.Data

@inject SignInManager<User> SignInManager
@inject UserManager<User> UserManager

@*<ul class="navbar-nav">
@if (SignInManager.IsSignedIn(User))
{
    <li class="nav-item">
        <a id="manage" class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Hello @UserManager.GetUserName(User)!</a>
    </li>
    <li class="nav-item">
        <form id="logoutForm" class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
            <button id="logout" type="submit" class="nav-link btn btn-link text-dark">Logout</button>
        </form>
    </li>
}
else
{

    <li class="nav-item">
        <a class="nav-link text-dark" id="register" asp-area="Identity" asp-page="/Account/Register">Register</a>
    </li>
    <li class="nav-item">
        <a class="nav-link text-dark" id="login" asp-area="Identity" asp-page="/Account/Login">Login</a>
    </li>
}
</ul>*@

@if (SignInManager.IsSignedIn(User))
{
@*    <li class="nav-item">
        <a id="manage" class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Hello @UserManager.GetUserName(User)!</a>
    </li>
    <li class="nav-item">
        <form id="logoutForm" class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Blog", new { area = "" })">
            <button id="logout" type="submit" class="nav-link btn btn-link text-dark">Logout</button>
        </form>
    </li>
*@

    <div class="col-md-4 top-forms text-center mt-lg-3 mt-md-1 mt-0 d-flex">
        <span class="mx-lg-4 mx-md-2 mx-1 mr-3">
            <a id="manage" class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">
                Hoşgeldin @UserManager.GetUserName(User)!
            </a>
        </span>
        <span>
            <form id="logoutForm" class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Blog")">
                <button type="submit" class="btn btn-link nav-link text-dark">
                    <i class="fas fa-sign-out-alt mr-2"></i> Log Out
                </button>
            </form>
        </span>
    </div>


}
else
{
    <div class="col-md-4 top-forms text-center mt-lg-3 mt-md-1 mt-0">
        <span class="mx-lg-4 mx-md-2  mx-1">
            <a id="login" asp-area="Identity" asp-page="/Account/Login">
                <i class="fas fa-lock"></i> Sign In
            </a>
        </span>
        <span>
            <a id="register" asp-area="Identity" asp-page="/Account/Register">
                <i class="far fa-user"></i> Register
            </a>
        </span>
    </div>
}

		
	
			


